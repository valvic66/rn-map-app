import React from "react";
import { Text, StyleSheet } from "react-native";

const TrackListScreen = () => {
  return <Text style={styles.text}>TrackListScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default TrackListScreen;
